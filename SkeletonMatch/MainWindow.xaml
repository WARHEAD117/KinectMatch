<Window x:Class="Microsoft.Samples.Kinect.SkeletonRecord.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SkeletonRecord" 
        Height="1930" Width="1100" 
        Loaded="MainWindow_Loaded"
        Closing="MainWindow_Closing">
    <Window.Resources>
        <SolidColorBrush x:Key="MediumGreyBrush" Color="#ff6e6e6e" />
        <SolidColorBrush x:Key="KinectPurpleBrush" Color="#ff52318f" />
        <SolidColorBrush x:Key="KinectBlueBrush" Color="#ff00BCF2" />
    </Window.Resources>
    <Grid Margin="10 0 10 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="1930" />
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        
        
        <Canvas Grid.Row="0" HorizontalAlignment="Left" Width="1100" Height="1930" ClipToBounds="True">
            <Image Source="{Binding ShowPic}" Canvas.Left="-250" Canvas.Top="-250" Stretch="None" />
        </Canvas>
        <Viewbox Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Height="342" Width="295" >
            <Image Source="{Binding ImageSource}" HorizontalAlignment="Center" VerticalAlignment="Center"  Stretch="UniformToFill" ClipToBounds="True" />
        </Viewbox>
        <StatusBar Grid.Row="1" HorizontalAlignment="Stretch" Name="statusBar" VerticalAlignment="Bottom" Background="White" Foreground="{StaticResource MediumGreyBrush}">
            <StatusBarItem Content="{Binding StatusText}" />
        </StatusBar>
    </Grid>
</Window>
